{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install -g tsx && pnpm install"
  },
  "deploy": {
    "startCommand": "tsx apps/api/src/db/migrate.ts && tsx apps/api/src/seed/index.ts && tsx apps/api/src/index.ts",
    "restartPolicyType": "ON_FAILURE"
  }
}
