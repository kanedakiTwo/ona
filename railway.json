{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install"
  },
  "deploy": {
    "startCommand": "npx tsx apps/api/src/db/migrate.ts && npx tsx apps/api/src/seed/index.ts && npx tsx apps/api/src/index.ts",
    "restartPolicyType": "ON_FAILURE"
  }
}
